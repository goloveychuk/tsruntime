{
  "name": "tsruntime",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "rimraf dist && tsc --pretty"
  },
  "license": "MIT",
  "devDependencies": {
    "reflect-metadata": "^0.1.10",
    "rimraf": "^2.6.1",
    "typescript": "^2.3.0"
  }
}
